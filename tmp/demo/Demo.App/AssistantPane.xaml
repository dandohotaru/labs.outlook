<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="Demo.App.Shared.Controls.AssistantPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400"
             Background="#202020">

    <UserControl.Resources>
        <!-- Define custom button style with rounded corners using ControlTemplate -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="BorderBrush" Value="#505050"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>

            <!-- ControlTemplate for rounded corners -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#202020" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#303030" Padding="10" Grid.Row="0" CornerRadius="5">
            <TextBlock Text="🧠 Email Assistant"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"/>
        </Border>

        <!-- Response TextBlock (stretches across available area) -->
        <Border Grid.Row="1" BorderBrush="#505050" BorderThickness="1" Background="#252525" Padding="10" CornerRadius="5">
            <TextBlock x:Name="ResponseTextBlock" 
                       TextWrapping="Wrap" 
                       Foreground="White"
                       VerticalAlignment="Stretch"
                       HorizontalAlignment="Stretch" />
        </Border>

        <!-- User Input Area -->
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <!-- Prompt TextBox with rounded corners -->
            <Border BorderBrush="#505050" BorderThickness="1" CornerRadius="5" Background="#252525">
                <TextBox x:Name="PromptTextBox" FontSize="14" Padding="10"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Background="#252525"
                         Foreground="White"
                         BorderThickness="0"/>
            </Border>

            <Grid HorizontalAlignment="Stretch" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left-aligned Buttons (Redo and Accept) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- Redo Button (with rounded corners) -->
                    <Button Style="{StaticResource RoundedButtonStyle}" Click="RedoButton_Click">
                        <TextBlock Text="&#xE7A6;" />
                    </Button>

                    <!-- Accept Button (with rounded corners) -->
                    <Button Style="{StaticResource RoundedButtonStyle}" Click="AcceptButton_Click">
                        <TextBlock Text="&#xE8FB;" />
                    </Button>
                </StackPanel>

                <!-- Dropdown Button (Near Send) -->
                <Button Grid.Column="2" Style="{StaticResource RoundedButtonStyle}" Click="DropdownButton_Click">
                    <TextBlock Text="&#xE736;" />
                    <Button.ContextMenu>
                        <ContextMenu x:Name="DropdownMenu">
                            <MenuItem Header="Reply to this email politely, acknowledging the sender’s request" Click="MenuItem_Click"/>
                            <MenuItem Header="Summarize the main points of this email for a quick overview." Click="MenuItem_Click"/>
                            <MenuItem Header="Write a thank-you message for the information provided in this email" Click="MenuItem_Click"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <!-- Send Button (Right-aligned) -->
                <Button Grid.Column="3" Style="{StaticResource RoundedButtonStyle}" Click="SendButton_Click">
                    <TextBlock Text="&#xE768;" />
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
